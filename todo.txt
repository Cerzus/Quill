- ALWAYS: Address TODO's
- ALWAYS: Do not use !important in CSS
- ALWAYS: Add more settings to config
- ALWAYS: Allow all non-getter methods to be chainable
- Add tests
- Get rid of fake private methods (methods starting with "_") (take a look at QuillPlot for inspiration)
- Panel: highlight topmost ("active") panel
- Panel: Improve size and position functionality
- MenuBar: Disable/enable MenuItems
- Allow Wrapper element as child of MenuBar and Menu elements
- Allow Wrapper element as child of Tabs element?
- Test Wrapper everywhere it's applicable (especially when traversing the parent/children hierarchy)
- Test Row element with different kinds of child elements
- Test "add_children" methods
- Focus Panel on middle mouse button down; and therefore also close menus etc.
- Make a choice between top/left and x/y
- Sanitize API inputs
- Sanitize end-user inputs
- Plots/canvases etc.: Make "dirty" and only update once (and only when visible)
- Align inputs and labels
- Add Child element for scrolling content
